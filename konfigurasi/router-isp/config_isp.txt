# TEMPLATE KONFIGURASI R-ISP - DISESUAIKAN PADA PROGRES 2
# Nama interface pada GNS3 dapat berbeda. Periksa dengan perintah: ip addr

# Rencana pengalamatan:
# eth0 -> 203.0.113.2/30  (menuju R-INTERNET / Upstream)
# eth1 -> 198.51.100.1/30 (menuju R-CPE Pelanggan)

# Contoh konfigurasi sementara pada Ubuntu Router:
# sudo ip addr add 203.0.113.2/30 dev eth0
# sudo ip addr add 198.51.100.1/30 dev eth1
# sudo ip link set eth0 up
# sudo ip link set eth1 up
# sudo sysctl -w net.ipv4.ip_forward=1
# sudo ip route add default via 203.0.113.1

# Simpan hasil konfigurasi final dan screenshot pada folder ini setelah P2 selesai.
